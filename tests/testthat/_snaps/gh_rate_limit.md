# gh_rate_limit() with no arg is deprecated

    Code
      out <- gh_rate_limit()
    Condition
      Warning:
      The `response` argument of `gh_rate_limit()` is deprecated as of gh 2.0.0.
      i Please use `gh_rate_limits()` instead.

